export type TileSpec = {
  id: string;
  x: number;
  y: number;
  z: number;
  row: number;
  col: number;
  value: number;
};

export type TilePosition = {
  x: number;
  y: number;
  z: number;
};
